schema: ./schema/typeDefs.graphql
overwrite: true
generates:
  ./types/graphql.d.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      maybeValue: T
      mappers:
        AuthenticatedUser: ./customTypes#AuthenticatedUser
